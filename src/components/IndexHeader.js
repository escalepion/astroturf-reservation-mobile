import React from 'react';
import { View, Button } from 'react-native';

const IndexHeader = () => {
    return (
        <View>
            <Button 
            title="Ekle"
            onPress={() => {}}
            />
        </View>
    );
};

export default IndexHeader;